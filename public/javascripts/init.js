
//initializing in main doesn't work because these elements are not created at that point somehow
const step_duration = $('#stepDuration');

const cb_colored = document.getElementById('cb_colored');   //$('#cb_colored');
const cb_edge_labels = document.getElementById('cb_edge_labels');   //$('#cb_edge_labels');
const cb_classic = document.getElementById('cb_classic');   //$('#cb_classic');
